# Phase 6 — AI Agent FCE

This directory will house the second, independently code-hashed Flare Compute Extension (FCE) for:

- Performance scoring (Sharpe, drawdown, consistency)
- Strategy classification (momentum / mean-reversion / yield-arb)
- FDC Web2Json attestation requests
- On-chain leaderboard updates via `MirrorLeaderboard.updateScore()`

**Status:** Placeholder for Phase 6. See [docs/phaseImplementation.md](../docs/phaseImplementation.md).
