# Deploy scripts

Core contract deployment lives in [contracts/scripts/01-deploy-core.ts](../../contracts/scripts/01-deploy-core.ts).

Run from repo root:

```bash
npm run deploy:core
```

Requires funded deployer in `.env` (see `npm run provision:faucet-checklist`).
